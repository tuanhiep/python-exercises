[project]
name = "python-exercises"
version = "0.1.0"
description = "Python exercise bank for implementation practice."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
